# Inventory file
# If hostname is used, there will be problems after 
all:
  children:
    test:
      hosts:
        debianserver1.fritz.box:
          new_hostname: "debianserver1"
          new_domain: "fritz.box"
    rpis:
      hosts:
        rpi1.fritz.box:
        rpi2.fritz.box:
    webservers:
      hosts:
        debianserver1.fritz.box:
          debian_release: "buster"
